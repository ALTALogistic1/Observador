# Dépendances d'exécution (voir aussi pyproject.toml, source de vérité pour packaging)
SQLAlchemy>=2.0
requests>=2.31
PyYAML>=6.0
beautifulsoup4>=4.12
click>=8.1
rapidfuzz>=3.6
python-dateutil>=2.9
openpyxl>=3.1
pdfplumber>=0.11
cffi>=1.16
Flask>=3.0
# `>=25.1` et non `>=22.0` : le drapeau `--no-control-socket` qu'emploie
# deploiement/falkye-web.service n'existe qu'à partir de cette version. Avec une
# version antérieure, le service refuserait de démarrer sur un argument inconnu
# — un échec bruyant plutôt qu'un retour silencieux de l'erreur corrigée, ce qui
# est le bon comportement.
gunicorn>=25.1

# Dépendances de développement / tests
pytest>=8.0
pytest-mock>=3.14
responses>=0.25
